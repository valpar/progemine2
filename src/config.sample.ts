const config = {
    jwtSecret: 'secretpassword',
    db: {
        host: 'localhost',
        user: 'root',
        password: 'mockpassword',
        database: 'blog'
    }
};

export default config;