const config = {
    jwtSecret: 'secretpassword',
    db: {
        host: 'localhost',
        user: 'root',
        password: 'mockpassword',
        database: 'movieblog'
    }
};

export default config;